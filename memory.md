# memory.md — reported bugs & requests (READ THIS EVERY SESSION)

Purpose: the user reports bugs/requests here so I don't forget or re-break things.
**Before touching the captions/auto-edit/panel UI, read this file.** Update status
when something is fixed. Newest at top.

## Hard rules (don't violate)
- **HOST.JSX STALE-AFTER-UPDATE**: CEP only loads host.jsx (via manifest ScriptPath) when the extension first OPENS. Auto-update writes the new host.jsx to disk but the running ExtendScript ENGINE keeps the OLD one → new host functions error ("EvalScript error", e.g. the ccSelectionSig spam every 500ms = "not working"). FIX shipped: `ensureFreshHostScript()` on panel init reads host.jsx from disk (`_fs` + `CSInterface.getSystemPath(EXTENSION)`) and re-evals it into the engine when the loaded `ccVersion()` ≠ the on-disk `HOST_JSX_VERSION`. So after this lands, host updates self-apply on the next panel HTML reload (no manual reopen). When debugging "host function not working after an update", suspect a stale engine FIRST. Any code calling a NEW host fn must tolerate the "EvalScript error" string from evalES (it returns that string, NOT null).
- CEP = old Chromium: use **mouse events** (pointerdown doesn't fire). **NO `backdrop-filter`** (crashes Premiere).
- host.jsx is **ES3 only**: `var`/`function` only, no `const`/`let`/arrow/template-literals/`.includes`; wrap calls in `_ccSafe`; never `new Time()`.
- Don't break login / auto-edit / chat / renders / website.
- Verify in the browser (Playwright @ http://127.0.0.1:8799) before claiming done. Measure, don't assume.
- Captions tab follows **Claude design**: warm darks, scarce coral accent, no glows/gradients, shadow-rare.

## Open / watch
- [ ] Auto-Edit per-graphic CHANGE (revise one graphic, swap in place) — code done, **needs a live Auto-Edit run** to confirm: after a run, each graphic shows a "Change" button; typing e.g. "different background" re-renders just that one (`/autoedit/rerender`) and swaps it on the timeline (`ccAutoEditReplace` lifts the old clip on its track at its second, overwrites the new). Only works in the SAME session (bridge cache holds the plan+style, 30-min TTL).
- [ ] Auto-Edit motion-graphic TIMING (graphic must never end before the sentence) — code done, **needs a live Auto-Edit run** to confirm graphics now hold through the whole sentence and exit only at the very end.
- [ ] Auto-Edit animation placement (V-tracks, no overwrite) — code done, **needs live in-Premiere verification** (QE addTracks is build-dependent).
- [ ] Auto-Edit lower-third HEIGHT: first geometric fix jammed graphics to the very bottom edge (told Claude "bottom-anchored, anchor to bottom edge"). Reworked to a FLOATING lower-third: top bound (off face) + bottom bound (margin below, ~11% vertical) + explicit target CENTER (now 66% vertical (upper chest, matched a box the user drew) / 74% horiz; face line lowered to 54% vertical so the guard allows it that high). Graphic sits in the lower third with clear space below, not glued to the bottom. **Needs a live run** to confirm it matches the "a little up" target. Knobs in generateMomentsParallel: `targetCenterFrac`, `bottomMarginFrac`, `faceSafeTopFrac`.
- [ ] Auto-Edit face-avoidance — REWORKED to be deterministic (was: ask Claude to read frames + avoid the face → it still made a card over the chin). Now every overlay is HARD-constrained to a bottom band with an explicit pixel box (vertical: below 60% of height; horizontal: below 68%), full-frame cards forbidden, + a post-render ffmpeg alpha guard that detects intrusion into the face zone and retries once with a firmer bound (validated: compliant=0, intruding=109, limit=12). Face frames are now just a left/right bias hint. **Needs a live run** to confirm graphics land in the bottom third, clear of the face. If still high, lower `faceSafeTopFrac`/`FACE_ZONE_LIMIT` in generateMomentsParallel (the log prints the measured face-zone alpha per graphic).
- [ ] Panel laggy in Premiere (feels ~30fps): added GPU + frame-rate CEF flags to manifest; user says still laggy. Likely a CEP/Premiere host cap we can't override from the extension. NOT caused by our JS (smooth in a browser). Don't cap our animations to 30fps — user explicitly forbade it.
- [ ] Captions native/animated render correctness on a real clip — needs the user in Premiere.

## Fixed (this session)
- Captions: NEW professional "Fade Up" style (letters fade in + rise, word by word) — render `style:'fadeup'` in Captions.tsx (WordView splits the word into per-letter spans, ms-based interpolate so smooth at any fps; added to the CaptionStyle union AND the SIZE_FRACTION `Record<CaptionStyle,number>` — TS-typechecked clean). Preview mirrors it: capBuildLine builds `.cap-ch` letter spans for fadeup, capStyleWord animates each staggered (verified [0.33,0.13,0,0] cascade). UI: CAP_LOOKS.fadeup + fadeupkw, Quick Look badges "Fade Up"/"Fade Up + KW", STYLE card "Fade Up" (after Karaoke). 60fps: render already uses the SEQUENCE fps (capState.fps from ccGetSeqDims) — so 60fps on a 60fps timeline; fade-up is ms-based so smooth at 30 too.
- Captions Quick Looks reordered: professional/clean looks (Karaoke, Clean, Minimal+, Highlight, new "Editorial", Type-on) FIRST; loud TikTok ones (Pop, Beast, Beast Blue, Bold Yellow, Neon, Rainbow) moved to the RIGHT. New `editorial` preset (minimal style, off-white, tracked, no stroke).
- Captions clips no longer carry a silent audio track. `renderCaptions` was missing `--mute` (Remotion added silent stereo) and `splitCaptionClips` uses `-c copy` (copied it into every split). Fix: added `--mute` to the caption render args + `await stripAudioInPlace(outFile)` before returning (so the split source is clean and `-c copy` splits inherit no audio; `-c:v copy` preserves ProRes 4444 alpha). Bridge-only — plain ↻ refresh applies it. (Native SRT mode has no audio; unaffected.)
- Voiceover-only mode now CREATES a background (was floating text over the footage). `buildPrompt` uses a `vo` flag: voiceover-only graphics get a full-screen OWN background that COVERS the footage (the header, the `bg=transparent` line, and the "TRANSPARENT background CRITICAL" mandate are all conditional on `vo` — for vo it's "FULL-FRAME background, opaque alpha is fine, still ProRes 4444"). Normal overlays unchanged (transparent).
- Auto-Edit "Anything else?" box accepts a PASTED reference image: paste → saved to disk (`_saveBlobToDisk`, factored out of attachBlobAsReference) → thumbnail with × → path tracked on `aeWizard.refImages` → sent in `/autoedit/run {refImages}` → genOpts.refImages → buildPrompt adds a "REFERENCE IMAGE — Read each and mirror its look" block (like faceFrames). Cached so per-graphic Change reuses it. Bridge filters to existing files, caps 6. Verified in browser (paste→thumb→tracked→body→remove).
- Auto-Edit results card now has per-graphic DELETE + a DELETE ALL button (it only had Change). Delete removes the clip from the timeline (new ES `ccAutoEditRemove({track,atSec})` host 5.12 — lifts via QE `remove(false,false)`, no ripple) AND deletes the file (/delete-file) + clears the history entry. Delete-all loops it then removes the card. Uses the `track`/`placedAtSec` already on each record. Verified in browser (6 rows each w/ Delete + Delete-all; 1 delete = 1 lift + 1 file-delete; delete-all = card gone).
- Multi-version render: false "you're out of renders for the rest this month" message. The old code showed it whenever `made < versionCount`, but that also fires when a version simply FAILS to render (owner is never metered — log confirmed `rendering: 2` then `rendered: 1` = a render failure, not a quota). Now: "out of renders" note ONLY when `N < versionCount` (metering actually capped the batch); when `made < N` (some attempted versions failed) it says "X of N rendered — the rest didn't come through, give it another go." (Underlying occasional single-version render failure is separate/possibly transient.)
- Reopening an Auto-Edit history entry showed only 1 graphic instead of all N. `reopenConversation`'s renderEntry only rendered `entry.path` (the first). Now: if an entry has an `items[]` array (Auto-Edit), it renders a card for EVERY graphic (one addRenderPreview per item.file, in one Flimify bubble) — each importable/deletable, and the tab's "Delete all" reflects all. Also removed the `kind!=='autoedit'` skip in openConversationForClipPath so selecting an auto-edit clip opens the whole run. Verified: 6 items → 6 cards.
- Clip auto-opened conversation is now an EPHEMERAL "peek": on DESELECT the auto-opened tab CLOSES (returns to the user's original tab) instead of lingering. `_clipWatchAutoTabId` tracks the one peek tab (set only when reopenConversation CREATES a tab, not when switching to an existing real tab); closed on deselect and when peeking a different clip (only one peek at a time). GUARD: `_clipTabHasActivity` keeps the peek if the user did real work (in-flight render OR typed a draft) — never kills work. Verified: open→close-on-deselect, hop closes prev peek, draft keeps it.
- Flimify imports now land in ONE project bin "Flimify Graphics" (inside Premiere, not the OS folder). New ES3 helper `_ccEnsureBin(name)` (host 5.11) find-or-creates a top-level bin (matches by name + type===2/createBin); all 7 `importFiles(...)` calls now pass `(_ccEnsureBin(CC_BIN_NAME) || rootItem)` as the targetBin (render, auto-edit, replace, caption clips, SRT, preview, legacy). `_ccFindItemByPath(rootItem,...)` still finds items (recurses into the bin) so placement is unaffected; falls back to root if bin creation fails.
- Clip select/deselect was SLOW again (~2.4s) because the watch resolved the path via the heavy `ccGetSelectedClip` (~1.2s on a busy timeline) on every change, AND the new host (5.8) never loaded (the `evalScript(80KB string)` re-eval silently failed). TWO fixes: (1) `ccSelectionSig` (host 5.9) now returns the media PATH too — straight from `seq.getSelection()[0].projectItem.getMediaPath()`, no timeline walk — so the watch uses ONE cheap call for sig+path and NEVER calls ccGetSelectedClip on the fast path (verified: 0 ccGetSelectedClip calls on select/deselect). (2) `ensureFreshHostScript` now loads the host with ExtendScript `$.evalFile(path)` instead of passing the file as a string — reliable for big files, always-run on init, so host updates self-apply.
- Clip select/deselect → conversation made FASTER. `ccSelectionSig()` (host 5.8) now uses Premiere's `seq.getSelection()` FAST PATH (no timeline walk) — empty selection = instant, which fixes the slow DESELECT (the track-walk had to scan every clip on every track with no early-exit when nothing was selected, slow on busy timelines). Falls back to the track-walk on builds without getSelection(). Poll dropped to 500ms (was 2.5s → 700 → 500). The heavy `ccGetSelectedClip` path lookup only runs when the sig CHANGES (verified: unchanged = 0 heavy calls, change = 1, deselect needs none).
- Auto-Edit graphics now match normal renders on the timeline: (1) ROSE color label — `ccAutoEditApply` was missing `setColorLabel(6)` (came in blue/default); added it (host 5.6). (2) NO audio — the auto-edit render (`generateMomentsParallel` conclude) now calls `stripAudioInPlace(task.outFile)` (was only done for chat renders in `ensurePremiereImportable`), so the silent stereo track is dropped and no linked audio clip appears. Strip uses `-c:v copy -an` → ProRes 4444 alpha preserved (verified: yuva kept, audio gone). `ccAutoEditReplace` already set the label + reuses stripped files. EXISTING graphics from old runs stay blue+audio → re-run Auto-Edit to fix them.
- Deselect a clip → returns to where the user was before the auto-jump (home screen or the tab they were on). `_clipWatchReturnTabId` saved on the FIRST auto-jump (only if it changes tab; not when already on the clip's tab); restored on the deselect transition (was-selected→none); manual switchTab/newTab clears it (manual nav wins). State machine split into `_clipWatchApply(p)` for testability. Verified in browser: select→jump→deselect→home, manual-nav-cancels, already-on-it-no-op.
- Select a Flimify clip on the timeline → its conversation tab opens automatically. Gentle poll (`ccGetSelectedClip` every 2.5s, only when panel visible + idle + no modal + not mid-render). Match clip source path → history thread (exact then basename, incl. Auto-Edit items). Tabs carry `threadKeys[]` (tagged on render via addHistoryEntry + on reopen); switches to an existing tab or reopens (no duplicates); non-Flimify clips are a NO-OP. Suppresses `input.focus()` during the auto-switch (`_suppressAutoFocus`) so it never yanks keyboard focus out of Premiere. Setting `openClipConversation` (default ON) toggles it. Verified in browser: open/switch/dedup/basename/no-op all correct.
- "Delete all renders in this tab": trash button on the ACTIVE tab pill (shown only when it has render cards). Collects every `.render-preview[data-render-path]` in the tab, confirms, deletes each via /delete-file + clears history entries + removes cards. Render cards now carry `data-render-path`; `renderTabStrip()` is called after add/delete so the button appears/updates. Verified in browser (button shows w/ count, collects all paths).
- Delete option for generated animations: each render-preview card now has a **Delete** button (deletes the .mp4/.mov from disk + removes the card + drops the matching history entry), and the History per-entry Delete now also deletes the file(s) (with confirm). New bridge endpoint `POST /delete-file {path}` — SAFE: media-ext only, must be inside OUTPUT_DIR or a `*/output/*` folder under home, no `..`, files only, idempotent. Verified against the live bridge: blocks /etc/hosts, traversal, outside-output; deletes output media. (Bulk "Clear history" still does NOT delete files — only the per-entry Delete does.)
- Particles "going crazy" (flying fast): the particle bg motion was FRAME-based (`p.x += p.vx` per frame, assumes 60fps). The manifest CEF flags `--disable-frame-rate-limit` + `--disable-gpu-vsync` (added to fix the 30fps-feel lag) uncapped CEF's fps after a Premiere restart, so particles moved proportionally faster (4× at 240fps). Fix: delta-time motion in `Particles.frame(ts)` — `dt` in 60fps-frame units, `p.x += p.vx * dt`, clamped `dt<=4` for background gaps, `lastTs` reset in `start()`. Speed is now constant ~15.6px/s at any fps (simulated 30→240). Keeps high-fps smoothness; no pause, no fps cap (both user-forbidden). DON'T revert to per-frame velocity.
- Auto-Edit graphics OVERLAPPING each other: the timing fix (cover whole sentence + 1s tail) made graphics longer, and now they all sit in the SAME lower-third band, so two consecutive ones were on screen at once → visual collision. Fix: each graphic's duration is clamped to end ~0.25s BEFORE the next moment starts (`_nextStartAfter`). For sequential sentences this only trims the bonus tail (sentence still fully covered); only a genuinely overlapping plan gets shortened. Simulated: no overlaps, sentences still covered.
- Auto-Edit: aspect ratio is now AUTO-DETECTED from the active sequence (`ccGetSeqDims()` → width/height) — the "Aspect ratio?" question is dropped and `answers.ratio` is set automatically; a subtle chip shows the detected format ("Vertical 9:16 1080×1920 — detected from your sequence"). 4K sequences normalize to ≤1920 longest edge (keeps overlay renders fast; placement is fraction-based so absolute res doesn't matter). Falls back to the manual question if detection fails. Verified in the browser (question removed, answer set, chip renders). No host.jsx change — ccGetSeqDims already existed (used by Captions).
- Auto-Edit: questions modal opens at the TOP now (was opening scrolled down on the short CEP panel — user had to scroll up). `aeScrollQuestionsTop()` resets both scroll containers on show (immediate + rAF + 140ms). Verified: forced scrollTop 102 → reset to 0.
- Auto-Edit interview: added an OPTIONAL free-text box ("Anything else? (optional)") at the bottom of the questions modal. Writes to `aeWizard.answers.custom`; the bridge feeds it into BOTH moment selection (buildMomentGuidance auto-includes it) and every graphic's design prompt (genOpts.userExtra → buildPrompt). Cached with genOpts so per-graphic Change reuses it. Verified in the browser preview (renders, writes to answers, styled to match). Leave it blank and nothing changes.
- Auto-Edit per-graphic Change: after a run the panel lists each graphic ("Motion Graphic 1, 2, …") with a Change button; the user revises ONE ("different background") and only that one re-renders + swaps in place. New bridge endpoint `/autoedit/rerender {reqId, idx, change}` reuses the run's locked style/res/placement (cached `plan`+`genOpts`) and appends a CHANGE directive to the per-moment prompt; new ES3 `ccAutoEditReplace` swaps the clip in place (QE lift the old clip on its track at its second, DOM overwriteClip the new). Bridge result objects now carry `idx` (plan index) so a filtered graphic maps back to its moment.
- Auto-Edit timing: graphics no longer finish early. Root causes were (1) a hard **6s duration cap** (`Math.min(6, …)`) that truncated any sentence longer than ~5.4s by 2-3s, and (2) the render prompt saying "animate out **before** the end" (Claude faded it away early). Fix: `durationSec = max(2.8, min(20, speechDur + 1.0))` (covers the whole sentence + 1s tail), and the prompt now says HOLD fully visible for the entire duration, exit ONLY in the last ~0.4s, never clear the screen early. `m.endSec` confirmed = the moment's last-sentence end, so duration tracks real speech.
- Quick-Look badges: content-width cards + uniform 16px chip padding → every badge exactly 40px apart, consistent 15px font, no clipping. (Earlier uniform-WIDTH scaling was wrong — it caused font variance + clipping. Don't re-introduce it.)
- Words-per-line: lines now stay on ONE row + auto-fit-to-width (preview + render); default 3 words.
- Caption clips → high tracks (V19-V22), non-overlapping/editable.
- Custom searchable font dropdown (native <select> broke in CEP); bigger/bolder font previews.
- Gray (not orange) scroller scrollbars; Quick Looks tighter.
- Color picker mouse-event fix; compacted POSITION + color picker; header chip-left/X-right.
- Working overlay = corner chip (not full black); header hide-on-scroll; Style cards = horizontal scroller + arrows.
- Claude-design restyle of captions tab; vertical accurate inline preview; Weight + Shadow now actually show.
- All system fonts via bridge OpenType parser; render pipeline sped up (concurrency + parallel split).
- Multi-version results stream as each finishes.
- Auto-Edit history + All/Renders/Auto-Edit filter in the History panel.
- Unified log: `captions` is a first-class module. Version → 11.0.
